server:
  port: 8081
  servlet:
    context-path: /luckymoney

limit:
  minMoney: 100
  maxMoney: 9999
  description: 最少要发${limit.minMoney}RMB,最多只能发${limit.maxMoney}RMB
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://118.25.209.40:3306/luckymoney?characterEncoding=utf-8
    username: root
    password: 0913@LxcD
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
